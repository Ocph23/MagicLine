<!--<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converter ="clr-namespace:MagicLine"
             x:Class="MagicLine.CellView">
    <ContentView.Resources>
        <converter:ConventerBall x:Key="ballVisibility" ></converter:ConventerBall>
        <converter:ConventerBallSize x:Key="ballSize" ></converter:ConventerBallSize>
        <converter:ConventerBallColor x:Key="ballColor" ></converter:ConventerBallColor>
    </ContentView.Resources>
    <StackLayout x:Name="cellx" BackgroundColor="LightGray" Padding="{Binding Cell.Size, Converter={StaticResource  ballSize},
        ConverterParameter={x:Reference cellx}}">
        <Ellipse x:Name="ball" IsVisible="{Binding Cell.Size, Converter={ StaticResource ballVisibility }}"
                 Fill="{Binding Cell.Color, Converter={StaticResource ballColor}}"     
                 HorizontalOptions="StartAndExpand" VerticalOptions="StartAndExpand" >
        </Ellipse>
        <StackLayout.GestureRecognizers>
            <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"></TapGestureRecognizer>
        </StackLayout.GestureRecognizers>
    </StackLayout>
</ContentView>-->
